# API server
fastapi
uvicorn[standard]
pydantic-settings
prometheus_client

# HTTP client (overpass, osrm, narration)
httpx
aiohttp

# AI visibility filter
groq
openai

# Geospatial math (haversine, bearing, project_endpoint)
geopy
geographiclib

# Ray casting + UTM projection (visibility pipeline)
shapely
pyproj

# Environment variables
python-dotenv

# Supabase (auth + persistent profiles)
supabase>=2.0.0
psycopg2-binary

# Testing
pytest
pytest-asyncio
