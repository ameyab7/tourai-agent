# API server
fastapi
uvicorn[standard]
pydantic-settings
prometheus_client

# HTTP client (overpass, osrm)
httpx

# AI visibility filter
groq

# Geospatial math (haversine, bearing, project_endpoint)
geopy
geographiclib

# Ray casting + UTM projection (visibility pipeline)
shapely
pyproj

# Environment variables
python-dotenv

# Testing
pytest
pytest-asyncio
